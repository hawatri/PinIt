<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/add_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="12dp">

    <LinearLayout
        android:id="@+id/add_widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="8dp">

        <ImageView
            android:id="@+id/add_widget_type_icon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@mipmap/ic_launcher"
            android:contentDescription="Type" />

        <TextView
            android:id="@+id/add_widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="PinIt"
            android:textSize="14sp"
            android:textStyle="bold"
            android:singleLine="true"
            android:ellipsize="end"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:textColor="@color/widget_text_primary" />

        <ImageView
            android:id="@+id/add_widget_open_btn"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:src="@drawable/ic_widget_open"
            android:contentDescription="Open in app"
            android:padding="2dp" />
    </LinearLayout>

    <ListView
        android:id="@+id/add_widget_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/add_widget_panel"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone">

        <ImageView
            android:id="@+id/add_widget_panel_image"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:scaleType="fitCenter"
            android:contentDescription="Preview"
            android:visibility="gone" />

        <TextView
            android:id="@+id/add_widget_panel_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="@color/widget_text_secondary"
            android:maxLines="3"
            android:ellipsize="end"
            android:gravity="center_horizontal"
            android:paddingTop="4dp"
            android:paddingBottom="4dp" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/add_widget_empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center">
        <TextView
            android:id="@+id/add_widget_empty_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tap to choose a note"
            android:textSize="13sp"
            android:textColor="@color/widget_text_secondary" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/add_widget_action_btn"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:background="@drawable/widget_btn_background"
        android:layout_marginTop="6dp"
        android:visibility="gone">
        <ImageView
            android:id="@+id/add_widget_action_icon"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:src="@drawable/ic_widget_play"
            android:contentDescription="Action" />
        <TextView
            android:id="@+id/add_widget_action_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Play"
            android:textSize="13sp"
            android:textStyle="bold"
            android:paddingStart="8dp"
            android:textColor="@color/widget_text_primary" />
    </LinearLayout>

</LinearLayout>
